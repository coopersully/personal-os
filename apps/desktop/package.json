{
  "name": "@personal-os/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tauri build --bundles app --ci",
    "bundle:dmg": "bash macos/scripts/release.sh",
    "dev": "tauri dev",
    "typecheck": "tauri info",
    "test:native": "cargo test --manifest-path src-tauri/Cargo.toml && swift test --package-path macos"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
