# Typed API-client feature modules

Feature modules expose typed client operations grouped by domain. They share the
single authenticated HTTP transport but do not reimplement token, session, or
error handling.

See [`docs/engineering/feature-ownership.md`](../../../../docs/engineering/feature-ownership.md).
