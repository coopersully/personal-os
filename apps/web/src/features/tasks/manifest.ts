import { workspaceIdentities } from "@/components/workspace-identity";

/** Tasks-owned navigation metadata. The shell decides placement. */
export const tasksNavigationItem = workspaceIdentities.tasks;
